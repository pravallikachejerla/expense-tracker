{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Full-stack expense tracker with auth, budgets, standardized categories, charts, CSV export, recurring expenses, and comprehensive docs",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",


    "start:backend": "npm start --prefix backend",
    "generate:report": "node generate-report.js"
  },
  "dependencies": {
    "docx": "^8.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["expense", "tracker", "react", "express", "mongodb"],
  "author": "pravallikachejerla",
  "license": "ISC"
}