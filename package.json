{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Full-stack expense tracker with auth, budgets, standardized categories, charts, and CSV export",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "echo \"No root tests. Run in subdirs.\"",
    "start:backend": "npm start --prefix backend"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["expense", "tracker", "react", "express", "mongodb"],
  "author": "pravallikachejerla",
  "license": "ISC"
}